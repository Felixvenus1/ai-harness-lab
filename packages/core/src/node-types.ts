// Purpose: Declare canonical node type names and configuration contract stubs.
export type HarnessNodeType =
  | "input_validator"
  | "normalizer"
  | "model"
  | "schema_validator"
  | "fallback"
  | "logger";
