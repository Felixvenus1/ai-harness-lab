// Purpose: Render the button stub that initiates flow execution in the UI.
import type { JSX } from "react";

export function RunButton(): JSX.Element {
  return <button type="button">Run</button>;
}
